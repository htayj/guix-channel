# Copy to .goocastle/.env only when an adapter requires credentials.
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# OPENCODE_API_KEY=
# GH_TOKEN=  # host-only unless an explicit Gooflow capability requires it
